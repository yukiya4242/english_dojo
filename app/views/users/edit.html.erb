<%= render 'layouts/header' %>

<div class="signup-body">
  <div class="signup-page">
    <div class="form">
      <div class="mb-4">Edit Your Account??</div>
      <%= form_with(model: @user, local: true) do |f| %>
        <%= render "devise/shared/error_messages", resource: @user %>
        <%= f.text_field :username, autofocus: true, autocomplete: "UserName", class: "form-control", placeholder: "username" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "Email", class: "form-control", placeholder: "email" %>
        <%= f.password_field :password, autofocus: true, autocomplete: "password", class: "form-control", placeholder: "Password" %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "Password_Confirmation" %>

        <%= f.submit "Edit Your Profile!!", class: "form-submit" %>
        <p class="message">Back <%= link_to "Go To Top Page", root_url %></p>
      <% end %>
    </div>
  </div>
</div>

